---
title: "Books"
---
    
```{r setup,include=FALSE}
knitr::opts_chunk$set(cache=FALSE,echo=FALSE,fig.align='center',message=FALSE)
```

```{r message=FALSE, warning=FALSE, include=FALSE}
source('R/gs_auth.R')
source('R/utils.R')
source('R/books.R')
```


These are some books written by members of the meetup.

```{r books}
# get books data and convert to html
books <- get_current_books(gsheet_id)
bookOutput <- sample(purrr::pmap(list(books$Title, books$Authors, books$URL, books$ImageURL), displayBook))
htmltools::div(
    class = 'book-flexArea',
    bookOutput
)
```
